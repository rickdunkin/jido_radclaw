# Executor seam PR-4 — hardening residuals + item-7 close-out (camus C1-1)

## Context

Item 7 of `docs/plans/unadopted-next-ten/README.md` (the executor seam, camus C1-1) shipped PR-1..3; **PR-4** is the final PR: hardening residuals plus the item-level close-out. Its named content (README:746-751): the `needs_input` → gate-case mapping (today a step error at `forge_executor.ex:369-371` saying "gate mapping lands in PR-4"), the write-capable-stage sandbox requirement, and the remaining camus OQ-1 questions — (a) workspace materialization, (b) override precedence coordinated with AR-9's conditionally-put options shape. PR-4 also carries the deferred reconciliation: camus C1-1 has no Status line yet, and the PR-1/2/3 notes all say the source reconciliation waits for PR-4.

**Operator-ratified scope decisions (this session):**
1. **needs_input** = gate case + answer-feeds-next-attempt (ToolApprovals model): durable fingerprint-fenced case; step still errors (rides existing lanes, no composer park); an approved answer is consumed single-use by the stage's next attempt and injected into the vendor prompt. (Full composer park deliberately not built — no **production** runner on the executor path emits `:needs_input`; this PR's scripted test runners become its first emitters.)
2. **Write posture** = enforce-only this PR: `access`/`session_sandbox` knobs + the write⇒docker hydration invariant (raise), with `session_sandbox: :docker` refused at dispatch (the `{:forge, :custom}` pattern). **The Docker write build is the named follow-up plan** (sbx is installed but path translation, deposit-URL reachability from the microVM, stdin-EOF, and sbx auth are a PR-2-sized live-smoke-gated build).
3. **OQ-1(b)** = BUILD the stage-level executor override now (run-level force-`:in_process` stays design-pinned, unbuilt).
4. **OQ-1(a)** = pin **direct rw mount** as the write-case materialization answer (sbx workspaces mount rw natively; `--clone` is the rejected alternative). Read-only half already answered by shipping (direct `project_dir` as cwd/`--add-dir`).

Done means: all four units land, `mix precommit` passes (exit code + counts reported verbatim), **nothing committed/staged**.

---

## Unit A — `access` / `session_sandbox` knobs (write⇒sandbox requirement, enforce-only)

### A1. `lib/jido_claw/agent/templates.ex`
- `@vendor_config_keys` (:627) → add `:access`, `:session_sandbox` (name avoids the template-policy `:sandbox` VFS-jail axis, cf. `refuse_forge_sandbox_combo!` :703-714).
- Vendor clause of `normalize_executor_config!` (:600-606): after the `:workspace` default (:603), write defaults in (normalizing-validator pattern, defeats present-nil): `Map.put_new(config, :access, :read_only)` + `Map.put_new(config, :session_sandbox, :local)`.
- `validate_vendor_config!` (:629-646): add `validate_vendor_access!` (`:read_only | :write`), `validate_vendor_session_sandbox!` (`:local | :docker`) — shape of `validate_vendor_workspace!` (:648-656), reads via `Map.fetch!` (defaults now always present) — and the invariant `validate_write_requires_sandbox!`: `{:write, :local}` **raises** ArgumentError ("access: :write requires session_sandbox: :docker — a write-capable vendor session must be docker-backed"). Write+docker passes hydration.
- Generalize `hydrate_review_binding/3` (:419-434) → **`hydrate_executor_binding/3`** taking the full executor term: `:in_process` ⇒ `{:ok, {:in_process, %{}}}` (config dropped — Unit B rule); `{:forge, _}` ⇒ existing validate+normalize with ArgumentError→`{:error, msg}` rescue; other ⇒ `{:error, "invalid executor ..."}`. **Delete the old name, migrate all call sites** (reach trivial-forwarder rule): `review_independence.ex:382` (`overlay_binding`), `:644` (`resolve_executor`), the `templates_test.exs:698-749` describe, and the two moduledoc refs (`review_independence.ex:48`, `:181`). De-"review" the helper's docs. **Call-shape note:** the review-knob call sites hold a bare vendor kind (`parse_kind`) — they must wrap it as `{:forge, kind}` when calling the new helper; stage overrides (Unit B) pass the full executor term directly.
- Moduledoc updates: :99-112 (drop "no access/sandbox keys this wave / read-only hardwired"), the `@vendor_config_keys` comment :623-626.

### A2. `lib/jido_claw/orchestration/review_independence.ex` (YAML boundary)
- `@config_key_map` (:136-142): add `"access" => :access`, `"session_sandbox" => :session_sandbox` (JOIN, so the `review:` section expresses them uniformly; a write+local knob surfaces the hydration error loudly at both seams).
- Value coercion beside `@workspace_values` (:146) + `coerce_value/2` (:343-346): literal maps `%{"read_only" => :read_only, "write" => :write}` / `%{"local" => :local, "docker" => :docker}`; unknown strings pass through so the hydration enum message rejects them. Never `String.to_atom`.

### A3. `lib/jido_claw/skills/steps/forge_executor.ex` (dispatch refusal)
- Vendor `build_spec` clause (:156-177): prepend to the `with` chain, BEFORE `resolve_workspace_dir` (fail closed before any resource): `refuse_docker_session(config, template_name)` — `session_sandbox: :docker` ⇒ `{:error, "Step ... docker-backed vendor dispatch lands with the write build (session_sandbox: :docker not yet dispatchable)"}`.
- `build_runner_config` (:553-574): `access: :read_only` stays hardwired at :565; update the :550-552 comment with the invariant: only read_only+local can reach it (write forces docker at hydration; docker refuses at dispatch). Comment-link the knob at `session_spec/3` `sandbox: :local` (:200-209) and the two `start_session_ready(expected_backend: HostShell)` sites (:339, :452).

### A tests
- `test/jido_claw/templates_test.exs`: defaults written in for every vendor kind; `access: :write` accepted; bad enum values raise; **the invariant** (`write`+`local` raises `~r/docker-backed/`, `write`+`docker` hydrates); fix the now-stale pins — `%{access: :read_only}` leaves the unknown-key raise list (:663-673), the exact `^config` pins (:616-630, :708-714) become subset assertions including the two defaults; rename + rework the `hydrate_review_binding` describe → `hydrate_executor_binding` (add `:in_process` ⇒ `{:ok, {:in_process, %{}}}`, invalid-term `{:error, ...}` cases; the vendor-only knob restriction stays covered by `review_independence_test.exs:190`).
- `test/jido_claw/skills/steps/forge_executor_test.exs`: `session_sandbox: :docker` template ⇒ docker-refusal step error with **no session started** (the :295-308 no-resource pattern); existing read-only pins (:237-244) stay green.

---

## Unit B — stage-level executor override (OQ-1(b), built)

Mirror the AR-9 tier seam exactly ("one shape"): `WaveBuilder.tier_opts/1` (`wave_builder.ex:216-219`) is the conditionally-put pattern; `plan-arbiter` (`catalog.ex:224-249`) is the declarer precedent. **Precedence at BOTH seams: test `:agent_templates_override` > `.jido/config.yaml` `review:` knob > stage override > template binding.**

### B1. `lib/jido_claw/route_composer/stage.ex`
- `@type executor :: :in_process | {:forge, :fake | :shell | :codex | :claude_code | :custom}`; `executor: executor() | nil` in `@type t` (:92-108); `:executor` in the nil-default defstruct group (:110-117); moduledoc Fields entry + atom-safety note.
- Serialization (round-trip both directions, closed sets, never `String.to_atom`): `to_map/1` (:167) emits `nil | "in_process" | "forge:<kind>"`; `from_map/1` (:194) decodes via a literal `@executors` map in the `with` chain — unknown string fails the whole stage decode (existing `enum_from` contract). Blast radius: `Catalog.to_map/from_map` (`catalog.ex:552`, `:572` — the durable launch catalog in `WorkflowRun.config["catalog"]`, rehydrated at `route_composer.ex:834`) delegates per-stage; round-trip test must cover it. **MCP ripple:** the `jido://workflows/{name}` resource payload renders stages via `to_map` — the new `"executor"` key is an **additive served-output field, a MINOR bump per `surface_version.ex:11`'s own rules**: bump `SurfaceVersion.current()` → 1.1 + changelog entry + regenerate the golden fixture (Unit D).

### B2. `lib/jido_claw/route_composer/catalog_validator.ex`
- `@executor_terms` list near :71; `check_executor/2` added to `structural_for/2` (:117-133): nil ⇒ ok; valid term **only on `{:worker_template, _}` stages** (gate/verify/seed stages carrying one are rejected at load); invalid term rejected. `check_enum` shape at :191-197.

### B3. `lib/jido_claw/route_composer/wave_builder.ex`
- `add_stage_step/3` (:192-205): `] ++ tier_opts(stage) ++ executor_opts(stage)`; `executor_opts/1` beside `tier_opts/1`: `nil ⇒ []`, else `[executor: executor]` (never present-nil). Production catalog is all-nil ⇒ options byte-identical.

### B4. `lib/jido_claw/skills/steps/agent_step.ex`
- Read `executor_override = Keyword.get(options, :executor)` beside `catalog_stage_name` (:61); forward as a new trailing arg at :81. Skill-YAML steps and the saga cleanup `run/4` (:157) naturally pass nil. Document in the options moduledoc (:11-19).

### B5. `lib/jido_claw/skills/steps/agent_runner.ex`
- `run/6` → `run/7` with `executor_override \\ nil` (default args keep all existing arities); pass into `ReviewIndependence.apply_executor(template, template_name, context, executor_override)` (:91-92). `dispatch_executor` unchanged.

### B6. `lib/jido_claw/orchestration/review_independence.ex` (both seams)
- **Dispatch**: `apply_executor/3` (:365-374) → `/4` (`stage_executor \\ nil`), restructured: test-overridden ⇒ template unchanged; reviewer template ⇒ knob first, `{:ok, :default}` falls through to the stage overlay (**knob beats stage**); every other template ⇒ stage overlay or unchanged (nil override = zero config reads, byte-identical to today). Stage overlay = `Templates.hydrate_executor_binding(stage_executor, Map.get(template, :executor_config, %{}), template)` + the `:384` merge shape — an override failing hydration (e.g. `{:forge, :shell}` with no command) is a **step error**, never a silent fall-through.
- **Launch fence**: `effective_executor/2` (:629) and `resolve_executor/3` (:636-652) grow a `stage` arg; add the stage branch AFTER the knob branch, **guarded on `stage.executor` non-nil** (`stage_override?/1` — an unconditional `{stage.executor, template}` would mask the template binding for every normal production stage, turning template-level vendors into `nil`/`:indeterminate` and manufacturing false holds); only a non-nil override returns `{:ok, {stage.executor, template}}` (identity only — config hydration stays a dispatch concern, matching how knob config errors surface today); nil falls through to the template binding. Update both call sites: `stage_violations/6` :535 and `producer_vendor/3` :602 (both already have the `%Stage{}` in scope) — a producer stage's own override now feeds implementer-vendor identity.
- Comment the sharp edge at `overridden?` (:406-410): test-override precedence is **name-gated** — a template present in `:agent_templates_override` suppresses stage overrides on it by design.

### B7. Test support + tests
- `test/support/jido_claw/route_composer/fixtures.ex:30-48`: `TestFixtures.stage/1` accepts `:executor`.
- **First declarer end-to-end** (extend `test/jido_claw/eval/composer_forge_fake_case_test.exs`): a catalog stage with `executor: {:forge, :fake}` over an in-process template drives the fake arm through a real composer run (template must NOT be in `:agent_templates_override` — the name-gating edge; arm `:executor_fake_outputs` by template/stage name).
- `wave_builder_test.exs` (beside the AR-9 byte-identity guard :44-54): no `:executor` key when nil; present when declared.
- `review_independence_test.exs`: `apply_executor/4` precedence matrix (override > knob > stage > template; knob-beats-stage on reviewer; stage-beats-template elsewhere; `:in_process` override ⇒ config `%{}`; invalid override ⇒ `{:error, _}`).
- `composer_review_independence_test.exs` (:139 pattern): strict hold **triggered by a stage override** (producer overridden to `{:forge, :claude_code}` vs anthropic reviewer knob) + degraded pass; **nil-override preservation at the fence** — a catalog of executor-nil stages resolves every template binding exactly as today (no `:indeterminate` drift, no false hold).
- Stage/Catalog serialization round-trip incl. unknown-string ⇒ whole-stage nil; CatalogValidator accept/reject (incl. non-worker-stage rejection); shipped-catalog pin: every production stage `executor: nil`, wave options byte-identical.

---

## Unit C — `needs_input` → gate case (answer-loop)

No DB migration: `AgentCase.kind` is a plain `:text` column (`20260608193816_add_human_gates.exs:28`); the `one_of` (:341) is app-level, fed from `Gate.Kinds.all()`. `:consume` (:241-246) is kind-agnostic; `AgentCaseEvent` already has `:opened/:approved/:rejected/:consumed`.

### C1. Kind + gate module
- `lib/jido_claw/orchestration/gate/kinds.ex:33` → `@kinds [:tool_call, :plan, :irreversible_write, :review_stall, :needs_input]` + moduledoc bullet. (Propagates to `dsl.ex:80` + `agent_case.ex:341`.)
- NEW `lib/jido_claw/gates/needs_input_gate.ex` (`use JidoClaw.Orchestration.HumanGate`, `kind(:needs_input)`, title/description; no input fields — the answer rides `decision_comment`; no-op hooks, `ReviewStallGate` posture).

### C2. `lib/jido_claw/orchestration/agent_case.ex`
- `create :open_needs_input` (mirror `:open_review_stall` :167-184): accepts `[:workflow_run_id, :step_name, :details, :fingerprint, :session_id]`; `workflow_run_id` NOT required (run-bound for provenance only, no run status flip, no checkpoint); `validate(present(:fingerprint))` + non-blank match; sets kind/gate_module/status. `define(:open_needs_input)`; broaden the `:consume` description comment.

### C3. NEW `lib/jido_claw/orchestration/needs_input.ex` (producer, mirrors `ToolApprovals` tool_approvals.ex:154-260)
- **Identity vs FK split (the ToolApprovals precedent, pin-types-at-Ash-boundaries):** the fingerprint's `session_key = context[:session_uuid] || context[:session_id]` is identity-only; the persisted `AgentCase.session_id` (nullable **UUID FK**) is resolved from `session_uuid` ONLY (mirror ToolApprovals' resolve step) — never write the external session label string into the FK. **Same treatment for the run:** the Reactor context carries `%WorkflowRun{}` at `context[:workflow_run]` (`reactor_runner.ex:324`) while `open_needs_input` accepts a UUID — extract `run && run.id` explicitly in the scope builder.
- **`step_name` fallback:** `AgentCase.step_name` is non-null and `step_name` is nil on the generic/skill path — use `step_name || template_name` for BOTH the stored attr and the fingerprint term (stable on every path).
- `fingerprint(scope)` — **question-agnostic stage identity** (pre-flight runs before any question exists; LLM question text varies): `CanonicalHash.sha256_term({:needs_input_v1, tenant_id, identity_key, template_name, step_name_or_template})`, disjoint prefix vs tool-call fingerprints in the shared pending-fingerprint partial-unique index (:71-75). **Identity floor (no tenant-wide collapse):** `identity_key = session_key || workflow_run_id` — a session-less run degrades to run-scoped identity (dedup within the run still works; cross-run claim simply never matches, which is correct when no conversation identity exists); if BOTH are nil, **refuse to open** (Trace-warned, plain step error) rather than share pending/approved answers across unrelated same-tenant cron/MCP attempts.
- **First-question-wins on pending reuse (deliberate):** a reused pending case keeps its original question (retries may rephrase the same ask; per-retry detail updates would need a case-update action + event-type growth for no operator value). Document in the producer moduledoc + gate description.
- `raise_case(scope, question, opts)` — **best-effort**: `Ash.transact` + FOR-UPDATE `lock_by_fingerprint`; pending exists ⇒ reuse (retry dedup, no duplicate); else `AgentCase.open_needs_input` + `:opened` timeline event; unique-violation loser re-reads pending (`AshErrors.unique_violation?`); broadcast `RunPubSub.broadcast_gate_requested` only on fresh open, after commit. `details` = `Presentation.details(NeedsInputGate)` merged with `%{"question" => Patterns.redact(question), "resume_hint" => ..., "template" => ..., "injectable" => vendor? and session_keyed?}` (details must be redactor-safe, :359) — **`"injectable"` requires BOTH the vendor executor kind AND session-keyed identity**: session arms never claim, and a run-scoped-identity case (session-less fallback) can't be claimed by any later run, so promising injection there would be false. Surfaces key their copy on it. Any fault ⇒ Trace `:guardrail`-style warn + `:error` — **the step error still returns**.
- `claim_answer(scope)` — **fail-open** (LoopGuard facade posture — a DB fault must never block a working stage): FOR-UPDATE; approved-unconsumed **decided within `@answer_ttl` (24h module attribute)** ⇒ `AgentCase.consume` + `:consumed` event ⇒ `{:ok, decision_comment}`; pending/rejected/stale/none/fault ⇒ `:none` (rejected is never consumed; a later `needs_input` opens a fresh case since reject leaves nothing pending). **The TTL bounds the question-agnostic replay risk** — cross-run consumption within the window is the intended "answer survives the terminalized run" semantics; a stale approved case is left inert (visible in history, never consumed).
- Telemetry counter `emit_needs_input(event, outcome)` in `core/telemetry.ex` (mirror `emit_executor/2` :281-284) **plus the named metric definition in the counters list (:81 area)** — the emit alone exposes no rollup (`jido_claw.needs_input.total`).

### C4. `lib/jido_claw/skills/steps/forge_executor.ex`
- Build `ni = needs_input_scope(context, template_name, step_name, kind)` once in `run/6` (:123-135) — **`kind` is already destructured there (:124), giving the producer the executor kind `injectable` needs** (vendor? computed from `kind in [:codex, :claude_code]`; session_keyed? from the scope's own keys). Other keys per `reactor_runner.ex:320-332`: tenant `context[:tenant] || context[:tenant_id]`, `context[:actor]`, **both** `session_uuid` (FK resolution) and the composite `session_key` (fingerprint — the `ToolApprovals.fingerprint` precedent :106), `context[:workflow_run]` (`run && run.id` extracted for provenance). Thread to both arms → `map_error_result/3`.
- Replace :369-371: raise the case, step error names the gate id and carries the **redacted** question — `"Step ... failed: needs operator input (gate <id>): <Patterns.redact(q)>"` (fail-open variant omits the id, still redacts). The step error is its own persisted sink (subagent terminal path `agent_runner.ex:449`, route-failure formatting), so it gets the same redaction as `details["question"]`, never the raw text. `:blocked`/`:continue`/`:error` clauses unchanged (gain `_ni`).
- **Vendor claim**: in the vendor `build_spec` clause (:156-177), claim LAST in the `with` chain — after Unit A's `refuse_docker_session` and `resolve_workspace_dir` — **so a refused dispatch never burns the single-use answer**. Un-ignore `_step_name`. Pass `operator_answer` into `vendor_prompt/5` → `/6`.
- **Injection** (`vendor_prompt` :217-226): between `workspace_note` and `deposit_instruction` (deposit stays LAST — the P1a order pinned at `forge_executor_test.exs:275-293`): `operator_answer_block(nil) ⇒ nil` (prompt byte-identical); else `"An operator answered an earlier request you made for input. Use this answer to proceed; do not ask again:\n\n" <> answer`. Argv redaction already rides the runners' `PromptRedaction`.
- Session arm (shell/fake) raises the case, never claims (structural: claim lives in the vendor clause only).

### C5. `lib/jido_claw/orchestration/cases.ex`
- Kind-dispatched `decide_loaded` clause for `%AgentCase{kind: :needs_input}` placed **FIRST — before the run-less catch-all at :163, not merely before the default (:182)**: a needs_input case may carry `workflow_run_id: nil`, and the existing `decide_loaded(decision, agent_case, nil, ...)` shape-dispatch would otherwise eat it and bypass the answer guard (kind is the discriminator; run nil-ness is not). The branch reuses the run-less commit path `decide_tool_call/5` (:396-409) — flip + timeline event, no WorkflowEvent, no resume, no run terminal. Moduledoc note (:69-81).
- **Abandon gets an explicit `:needs_input` refusal, dispatched kind-FIRST:** web hides the button, but REPL `/gates abandon` remains. The refusal must branch on `kind: :needs_input` **immediately after the case load — BEFORE `ensure_workflow_case` (:211)**, which would otherwise return `{:error, :not_workflow_case}` for run-less cases and never reach a later `abandon_loaded/4` clause. Refusal returns `{:error, :not_abandonable}` ("reject the case instead"), with a friendly REPL message clause.
- **Non-blank answer guard (server-side):** `decision_comment` is optional today, but for `:needs_input` it IS the payload — an approve with a blank/missing answer must be refused in the `:needs_input` decide branch with `{:error, :answer_required}` (the `{:error, :incomplete_waiver}` precedent), never approved-then-consumed-empty. Reject requires no comment.

### C6. Surfaces
- REPL `lib/jido_claw/cli/commands/approvals.ex`: `print_case` clause for `:needs_input` (render `details["question"]` + resume_hint, the :218-220 idiom); decide result message clauses **keyed on `details["injectable"]`** — injectable approve ⇒ "Answer recorded — the next attempt of this stage injects it..." with the single-use + 24h-expiry copy; non-injectable approve ⇒ "Answer recorded for the operator record — this executor cannot receive injected answers"; reject ⇒ "Declined — no answer injected". The answer already rides `/gates approve <id> [comment]` (:56-63); a blank-answer approve gets a friendly `:answer_required` message (server guard is authoritative, uniform across injectable/non-injectable — the comment doubles as the record).
- Web `lib/jido_claw/web/live/approvals_live.ex`: kind block (:95 pattern) rendering question + resume_hint + an `answer` textarea, **with the injection promise shown only when `details["injectable"]`**; `handle_event("decide")` (:188-198) maps `params["answer"]` directly to `decision_comment` (fallback to existing fields path) with client-side non-blank validation on approve + a flash for the server `:answer_required` error; flash clause; hide Abandon for `:needs_input` (:137) — the run is already terminal.

### C7. Test support + tests (red first: the case-raised assertion fails against current behavior)
- `test/support/scripted_deposit_runner.ex`: a `:needs_input` script key ⇒ `run_iteration` returns `{:ok, Runner.needs_input(question)}`.
- `lib/jido_claw/forge/runners/static_fake.ex` (:21): pass an iteration-result map (atom `:status` key) through verbatim; else `Runner.done(out)` as today (coder fixtures use string keys — unaffected). Enables the session-arm/eval scripting.
- NEW `test/jido_claw/orchestration/needs_input_test.exs` (TenantCase, mirror `tool_approvals_test.exs`): fingerprint determinism + question-agnostic; raise ⇒ pending case (kind, provenance, redacted question, resume_hint, `injectable`) + `:opened` event — **the persisted `session_id` is the Session UUID (or nil), never the external session label, and `workflow_run_id` is the extracted `run.id`**; raise-while-pending ⇒ same case (fence; first question wins); approve → `claim_answer` ⇒ `{:ok, answer}` + consumed + `:consumed` event; second claim ⇒ `:none` (single-use); **stale approved case (back-dated `decided_at` past `@answer_ttl`) ⇒ `:none`, not consumed**; reject ⇒ `:none`, no consume, later raise opens fresh; **identity floor: session-less scope falls back to run-scoped identity (a different run's claim never matches) AND marks the case `injectable: false` even on a vendor arm; session-less AND run-less ⇒ refuses to open (no row)**; `Cases.decide` branch — **dispatched by kind even with `workflow_run_id: nil` (never eaten by the run-less catch-all)**, flip + event, no WorkflowEvent, **blank/missing-answer approve ⇒ `{:error, :answer_required}`, case stays pending**; **`Cases.abandon` on a needs_input case ⇒ `{:error, :not_abandonable}` for both run-less and run-bound**; nil `step_name` falls back to template_name (stored + fingerprint); fail-open on nil tenant (`:error`/`:none`, no rows).
- NEW `test/jido_claw/skills/steps/forge_executor_needs_input_test.exs`: (1) vendor needs_input ⇒ case + step error with gate id, **question redacted in BOTH sinks** (a secret-bearing question never appears raw in the step error or `details`); (2) retry-while-pending ⇒ no duplicate; (3) approve ⇒ next attempt injects (assert block position: after task, before `submit_structured_output`) + consumes; third attempt re-raises fresh; (4) reject ⇒ no injection; (5) session arm (`:fake`) ⇒ case raised with `injectable: false`, no claim; (6) `:blocked`/`:continue` pinned as-is (closing the untested-residual); (7) no-tenant context ⇒ step error returns, no case.
- Surface tests: REPL print/decide message incl. blank-answer refusal + the non-injectable copy branch + the abandon `:not_abandonable` message; web render (injection promise only when injectable) + answer→decision_comment + blank-answer validation/flash + hidden abandon.

---

## Unit D — docs + reconciliation (the item close-out)

1. **`docs/system/executor-seam.md`** (the only page whose `sources:` covers templates/forge_executor/agent_runner/review_independence): rewrite :37 + :57-64 (knobs + write⇒docker invariant + dispatch refusal ⇒ "write can't reach a live session this wave"); :73-75 (the needs_input mapping is now the gate case: raise + step error + vendor-only single-use injection); add the stage-level override to the dispatch/launch-fence sections (:132-173) with the precedence line; residuals :182-194 — close the PR-4 line, reverse "no per-stage executor override", replace with: **the Docker write build (named follow-up: sbx path translation, deposit-URL reachability, stdin-EOF, auth model, live smoke)**; needs_input full park gated on an interactive-runner producer; `review:`-YAML now translates the two new keys. Add `stage.ex`, `wave_builder.ex`, `catalog_validator.ex`, `orchestration/needs_input.ex` to `sources:` + `## Source map`. Bump `verified: 2026-07-07` + `verified_sha:` (current HEAD).
2. **`docs/system/tool-approval.md`** + **`docs/system/terminal-statuses.md`** (both source `cases.ex`): note the `:needs_input` kind-dispatched decide branch (incl. the `:answer_required` guard); update terminal-statuses' Gate.Kinds "named-not-built" note (:71). Bump `verified:` on both.
3. **MCP surface MINOR bump + `docs/system/mcp-server-surface.md`**: the `jido://workflows/{name}` resource payload gains the stage `"executor"` key (via `Stage.to_map`) — an additive served-output field, a **MINOR bump by `surface_version.ex:11`'s rules**: bump `SurfaceVersion.current()` → `1.1`, add the changelog entry, regenerate the golden fixture (the deliberate-bump discipline the golden test enforces), and record the payload addition on the page. Bump `verified:`.
4. **AGENTS.md** executor-seam Key Patterns bullet: "PR-3 of 4 shipped" → complete; fold the PR-4 contract sentence (needs_input gate case + single-use answer injection; write⇒docker enforced with docker dispatch refused pending the write build; stage-level override precedence). Keep the bullet tight — mechanics live on the page (`mix jidoclaw.system_docs.check` guards both directions).
5. **camus C1-1 reconciliation** (`docs/exploration/camus/FEATURES-WORTH-BORROWING.md`): insert the Status blockquote directly under the line-50 Recommendation, mirroring C1-3's format (:129-158): `> **Status: ✅ ADOPTED 2026-07-07** (next-ten #7, PRs 1–4). Shipped as ...` — digest of PRs 1–4 with "Corrections to this entry:" covering at minimum: the seam lives in `Skills.Steps.ForgeExecutor` + `AgentRunner` dispatch (not a bare AgentRunner branch) with the `executor_config` second key; first production declarer is operator config (`review:` section), not a committed template; sketch (c)'s invariant gained jido-native runtime vendor detection + degraded mode (camus had neither); sketch (d)'s requirement enforced fail-closed at hydration with docker dispatch refused pending the write build; sketch (e) landed as the answer-loop gate case (raise + single-use consume + next-attempt injection), the full park still gated on an interactive-runner producer; sketch (a)'s "stage-level override can follow later" landed NOW in the tier_opts shape (precedence test > knob > stage > template); C3-1 resume unported; fresh-session-per-round structural + pinned.
6. **camus OQ-1** (:517): append the `✅ ANSWERED` treatment (OQ-4 format, :520): (a) direct `project_dir` shipped read-only; write-case pinned **direct rw mount** (sbx workspaces rw-native; `--clone` rejected); (b) precedence built (test > knob > stage > template); run-level force-`:in_process` design-pinned, unbuilt (no declarer); (c) single-channel (answered by PR-2, now stamped); (d) answer-loop gate case, park deferred to a live producer.
7. **README item 7** (`docs/plans/unadopted-next-ten/README.md`): new PR-4 DONE blockquote (PR-1/2/3 note style) recording the deviations above; mark the PR-4 list entry (:746-751) DONE; table row :64 and heading :532 → `✅ DONE 2026-07-07`. PR-1/2/3 notes stay as written (historical release notes stay historical).

---

## Sequencing & verification

**Order: A → B → C → D** (B consumes A's `hydrate_executor_binding`; C composes into A's vendor `with` chain). Red/green discipline per unit: write the behavior-pinning test first where the behavior changes (needs_input case-raised; write+local raise; stage-override dispatch), confirm red, then implement.

**Gate (must pass before "done"):** `mix precommit` run directly — never piped through tail/head/grep — with the exit code and test counts reported verbatim. Covers format, `jidoclaw.compile_check` (zero warnings, empty allowlist), credo strict + reach + ExSlop at zero findings, dialyzer, full suite, `system_docs`/`jido_md`/`system_prompt` checks (JIDO.md/system_prompt are name-set checks — no tool/template/skill names change, so no edits expected there; system_docs enforces the Unit D page edits). **Additionally run `mix ash.codegen --check`** to prove the no-migration claim for Unit C (the `:needs_input` kind + `:open_needs_input` action are app-level only — `kind` is a plain `:text` column; if the check demands codegen, that assumption was wrong and the migration ships with the unit).

**Known verification traps** (from repo memory): one rotating full-suite flake (MemoryExport / collector crash-recovery / `:pg`) ⇒ re-run once, don't treat as regression — the same test failing twice is real; `assert match?(pat, x), "msg"` for message-bearing asserts; conditionally-put options everywhere (never present-nil); closed literal key translation at YAML sites; Ash code-interface `define` for new actions; ExSlop clone risk on the mirrored `apply_executor`/`resolve_executor` precedence conds — if flagged, extract one shared precedence resolver both shape differently.

**Do not commit or stage anything.**

## Explicitly out of scope (named, not silent)
- **The Docker write build** — the immediate follow-up plan: docker-backed vendor dispatch (sbx path translation, deposit endpoint reachability from the microVM, stdin-EOF equivalent, sbx auth/secret model, rw repo mount per the pinned materialization), live-smoke-gated like PR-2.
- The full `needs_input` composer park (gated on an interactive-runner producer) and run-level force-`:in_process` (design-pinned in Unit D docs).
